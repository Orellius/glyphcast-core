import { defineConfig } from 'vite'

// Single page: index.html is the live demo. No framework, no build step beyond
// what vite gives for free - the whole codec is hand-written TS with no runtime
// dependencies.
export default defineConfig({})
